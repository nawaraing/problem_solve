#include <iostream>
#include <limits.h>

using namespace std;

typedef			unsigned long long ull;

int		main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	int			n, w, a;
	ull			ret = 0;

	cin >> n >> w;
	cin >> a;
	for (int i = 0; i < n; i++) {
		
	}
	return (0);
}
