#include <iostream>
#include <queue>

using namespace std;

int			n, k, x, y, dx[4] = {0, 0, 1, -1}, dy[4] = {1, -1, 0, 0};


int		main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	return (0);
}